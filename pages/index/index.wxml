<!--index.wxml-->
<view class="container" style='padding-top:50px;'>
    <view class="userinfo">
        <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
        <block wx:else>
            <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" background-size="cover"></image>
            <text class="userinfo-nickname">{{userInfo.nickName}}</text>
        </block>
    </view>

    <view class='temp' style='top: 300px;'>
        <text hidden='{{!pick.show}}'> {{pick.name}} </text>
    </view>

    <view class="usermotto">
        <view>
            <image src='../../images/shake.svg' style="width: 50px; height: 50px;" bindtap='findRestaurant'></image>
        </view>
    </view>
    <view>
        <text style='padding:5px;font-size:14px;color:gray;'>开始发现身边的美食吧</text>
    </view>

    <view wx:for="{{restaurants}}" wx:for-item="item">
        <text class="temp" id="restaurant-{{index}}" hidden="{{!item.show}}" style='top:{{item.top}};left:{{item.left}};color:{{item.color}};fontSize:{{item.fontSize}};text-overflow: ellipsis;overflow:hidden;white-space:nowrap;'>{{item.name}}</text>
    </view>
</view>